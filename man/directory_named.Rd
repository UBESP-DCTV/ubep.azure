% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{directory_named}
\alias{directory_named}
\title{The rows that carry this person's name}
\usage{
directory_named(directory, first_name, last_name)
}
\arguments{
\item{directory}{A directory frame as \code{directory_users()} returns.}

\item{first_name, last_name}{The names the register row declares.}
}
\value{
A logical vector, one per row.
}
\description{
The surname is the criterion and the given name confirms it \strong{only when the
account carries one}. An account with a surname and no given name stays in
play: it is not a different person, it is a person we know less about, and
dropping it would let a homonym through unseen.
}
\details{
An account with no surname at all matches nobody. The name is what this
question is asked with, and an account that does not answer it cannot be the
one — which is why the sweep asks Graph for \code{givenName} and \code{surname}.

Folded with \code{clean_string()}, the same way \code{compose_upn()} folds a name, so
what counts as the same name here and what a composed UPN looks like cannot
drift apart.
}
\keyword{internal}
