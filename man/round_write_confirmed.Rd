% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_write_confirmed}
\alias{round_write_confirmed}
\title{Did the instance show what the write asked for}
\usage{
round_write_confirmed(operation, observed)
}
\arguments{
\item{operation}{\code{"apply"} or \code{"revoke"}, from the batch that was written.}

\item{observed}{What \code{round_observed()} returned for this entry.}
}
\value{
\code{TRUE} when the write is confirmed: the pair is there after an
apply, or gone after a revoke.
}
\description{
Confirmation is by presence and not by field-by-field equality, and the
difference is deliberate: REDCap normalizes what it stores — a role name's
spacing, a case — and a row that had to match exactly could become
impossible to confirm and would be retried for ever. Presence is the fact
the write was about. Disagreement on the fields stays visible in
\code{applied_as}, and the next round's diff is where the design acts on it.
}
\keyword{internal}
