% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{upn_proposal}
\alias{upn_proposal}
\title{Propose the first free UPN with a numeric suffix}
\usage{
upn_proposal(composed, taken)
}
\arguments{
\item{composed}{The UPN \code{compose_upn()} built, already taken.}

\item{taken}{Normalized UPNs held by the whole tenant — guests included,
because uniqueness is tenant-wide and not a property of the candidates.}
}
\value{
A single UPN, free in the directory that was swept.
}
\description{
The convention was fixed by UBEP on 2026-08-15: \code{nome.cognome.2}, \code{.3}, and
so on, taking the first free one. It reads as a naming detail and is what
makes a collision workable — without a proposal the row hands a person two
questions and no material, and with it exactly one is left, the one only the
referent can close.
}
\details{
The search terminates because each attempt is a value not yet in \code{taken},
and \code{taken} is finite.
}
\keyword{internal}
