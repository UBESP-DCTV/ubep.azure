% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_client.R
\name{module_revoke}
\alias{module_revoke}
\title{Remove the rights a set of people hold in a set of projects}
\usage{
module_revoke(
  server,
  secret,
  requests,
  dry_run = TRUE,
  registry = tested_fingerprints()
)
}
\arguments{
\item{server}{Hostname of the instance, optionally followed by the path
REDCap is mounted under, as in \code{"host.example.org/redcap"}. The fleet is
not uniform on this point, so the mount cannot be assumed.}

\item{secret}{Shared secret, sent as the \code{X-UBEP-Secret} header.}

\item{requests}{List of requests; empty reads the whole instance.}

\item{dry_run}{Logical; the module writes only on an explicit \code{FALSE}.}

\item{registry}{Tested fingerprints, see \code{check_fingerprint()}.}
}
\value{
A list with \code{ok}, \code{errors}, \code{payload} and \code{gate}.
}
\description{
Carries the pair and nothing else: revocation does not care which rights
are there, and sending them would suggest it does.
}
\details{
A write (\code{dry_run = FALSE}) must concern a single \code{project_id} across all
\code{requests}; the module rejects a mixed-project write with \code{400} and code
\code{INTERNO}, because \code{PROJECT_ID} is an immutable per-process PHP \code{define()}
that selects which project's log gets the trace, so a write spanning two
projects would leave one of them untraced. That gate lives in the module,
not here. Reads and dry runs are free to span more than one project.
}
\keyword{internal}
