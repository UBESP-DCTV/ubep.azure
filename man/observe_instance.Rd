% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_observe.R
\name{observe_instance}
\alias{observe_instance}
\title{Turn one instance's \code{state} reply into a single observation row}
\usage{
observe_instance(server, state, today = Sys.Date())
}
\arguments{
\item{server}{The instance name.}

\item{state}{What \code{module_state()} returned: a list with \code{ok}, \code{errors},
\code{payload} and \code{gate}.}

\item{today}{The date expirations are compared against. Injected rather than
read from the clock so a fixture cannot turn red on a calendar date for a
reason unrelated to the code.}
}
\value{
A one-row data frame.
}
\description{
Pure: no network. The v1 of the job observes and does not compare, so this
never touches \code{provisioning_diff()}. That is not a disabled write path but a
missing comparison: with no register, every real pair would be classified
\code{revocato}, so the diff is not dangerous but empty of information — while
version, gate, reachability and expirations are properties of reality alone
and are available now.
}
\details{
A server that cannot be reached becomes a row rather than an exception, for
the same reason \code{provisioning_audit()} does it: one instance being down must
not hide the state of the others.

\code{scadute} counts pairs REDCap should already be refusing. It is the one of
the three drifts that needs no desired state, and the boundary is inclusive
because REDCap denies on \code{expiration <= TODAY} — the day written is already
interdicted.
}
\keyword{internal}
