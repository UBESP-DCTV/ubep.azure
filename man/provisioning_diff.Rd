% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_diff.R
\name{provisioning_diff}
\alias{provisioning_diff}
\title{Compare the desired state with the real one}
\usage{
provisioning_diff(desired, actual)
}
\arguments{
\item{desired}{List of validated requests.}

\item{actual}{List of rows as returned by the module's \code{state}.}
}
\value{
A data frame with one row per pair.
}
\description{
The engine of the channel, and the one piece that decides what will actually
happen. Pure: no network, no state of its own.
}
\details{
The unit of comparison is the (user, project) pair, not the user: the same
person may legitimately hold different rights in different projects, and
REDCap stores one row per pair.

A field wanted absent but present in reality is an update, never a noop.
That follows from \code{apply} asserting the full state rather than a delta, so
a DAG nobody asked for has to show up as a difference instead of being
tolerated.
}
\keyword{internal}
