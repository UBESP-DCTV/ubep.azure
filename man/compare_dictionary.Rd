% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register.R
\name{compare_dictionary}
\alias{compare_dictionary}
\title{Compare a project's dictionary against the packaged one}
\usage{
compare_dictionary(actual)
}
\arguments{
\item{actual}{The dictionary read back from the live project, in the same
eighteen-column shape \code{register_dictionary()} returns.}
}
\value{
A list with \code{conforms} and \code{differences}, the latter a character
vector of \code{CODE:field} strings. Shaped like \code{compare_readback()} because
it answers the same kind of question.
}
\description{
The packaged CSV is the schema and REDCap imports it as it is, so the two
start identical. What this reads is whether they still are: an import that
dropped a field, a hand edit on the form, a field type loosened to get past
a validation complaint.
}
\details{
None of those raise anywhere else. \code{request_from_row()} reads a missing
column as "not set" rather than as an error, and the channel ignores fields
it does not know — which is correct behavior and also the reason drift here
is silent. The comparison is the only thing that looks.

The codes it can report, and why each is not cosmetic:
\itemize{
\item \code{DIZIONARIO_CAMPO_ASSENTE} — the request loses that field without saying
so;
\item \code{DIZIONARIO_CAMPO_IN_PIU} — breaks nothing, and is direct evidence that
somebody edited the form by hand;
\item \code{DIZIONARIO_TIPO_DIVERSO} — a \code{request_status} gone free-text turns a
typo into a request to grant what somebody asked to revoke;
\item \code{DIZIONARIO_SCELTE_DIVERSE} — same type, and the value that revokes is
gone;
\item \code{DIZIONARIO_READONLY_CADUTO} — a requester can type \code{applied} into the
outcome, and the register carries a success nobody produced.
}
}
\keyword{internal}
