% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register.R
\name{log_creators}
\alias{log_creators}
\title{Who was authenticated when each of these rows was created}
\usage{
log_creators(events, records)
}
\arguments{
\item{events}{The creation events, as \code{register_log()} returns them when
asked with \code{logtype = "record_add"}. Handing it a wider log would answer
with whoever last touched the row instead of whoever made it.}

\item{records}{The record ids to answer about.}
}
\value{
A character vector as long as \code{records}, holding the username or
\code{""} where no creation event names that row. \code{""} is not "nobody made
it": it is "the log did not say", and the caller decides what that means.
}
\description{
The answer the register's own fields cannot give. \code{requested_by} is filled
in by \verb{@USERNAME}, and an action tag governs the form REDCap draws: an
import draws no form, so it writes into that field whatever it is handed.
Measured on 2026-09-02 on a live instance, an import creation logs
\code{username} as the account that was authenticated and carries the forged
name only inside \code{details}, where it is a string somebody chose rather than
a fact REDCap established.
}
\details{
It reads \code{record} and \code{username} and nothing else, which is what keeps it
apart from \code{log_last_author()}. That one searches \code{details} for a field
pattern because it asks "who wrote this field"; this one asks "who made
this row", and the caller has already had REDCap answer only with
creations. Matching the prose of \code{action} would be the third way and the
wrong one: the same act reads \verb{Create record 7} from the form and
\verb{Create record (import) 20} from an import.

The most recent event wins, and which one that is comes from the order of
the answer rather than from the timestamp -- REDCap's own, newest first,
the same rule \code{log_last_author()} follows. A record id normally carries one
creation; it can carry two when an id was deleted and made again, and then
the row that exists now is the one made last.
}
\keyword{internal}
