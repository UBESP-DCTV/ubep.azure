% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_client.R
\name{module_call}
\alias{module_call}
\title{Call one operation on one instance}
\usage{
module_call(
  server,
  secret,
  operation,
  requests = list(),
  dry_run = TRUE,
  registry = tested_fingerprints(),
  declare = certified_fingerprints(registry)
)
}
\arguments{
\item{server}{Hostname of the instance, optionally followed by the path
REDCap is mounted under, as in \code{"host.example.org/redcap"}. The fleet is
not uniform on this point, so the mount cannot be assumed.}

\item{secret}{Shared secret, sent as the \code{X-UBEP-Secret} header.}

\item{operation}{One of \code{"state"}, \code{"apply"}, \code{"revoke"}.}

\item{requests}{List of requests; empty reads the whole instance.}

\item{dry_run}{Logical; the module writes only on an explicit \code{FALSE}.}

\item{registry}{Tested fingerprints, see \code{check_fingerprint()}.}

\item{declare}{Fingerprints the caller states it has been tested against.
The module refuses to write when its own is not among them, so an empty
declaration writes nothing. Reads and dry runs ignore it.}
}
\value{
A list with \code{ok}, \code{errors}, \code{payload} and \code{gate}.
}
\description{
The only function in the package that speaks to a REDCap server, and the
only one that builds the URL. Keeping the base normalization here is what
stops the three operations from each carrying their own copy of it.
}
\keyword{internal}
