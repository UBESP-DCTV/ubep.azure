% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_fingerprint.R
\name{check_fingerprint}
\alias{check_fingerprint}
\title{Confirm the instance surface is one we have tested against}
\usage{
check_fingerprint(payload, registry = tested_fingerprints())
}
\arguments{
\item{payload}{Parsed module response.}

\item{registry}{Data frame of tested fingerprints.}
}
\value{
The effective gate, as a single string.
}
\description{
The major says which code path applies; the fingerprint says whether that
path is still the one we tested. An instance can be in the right major and
have a changed surface — the case no version comparison can see.
}
\details{
Downgrades only: a fingerprint can move an instance from \code{collaudata} to
\code{non_collaudata}, never the other way. An unknown fingerprint is not a
failure, it is a version waiting to be tested, and downgrading is how the
system asks for that instead of writing blind.

A missing or malformed \code{surface_fingerprint} downgrades the same way: an
absent field makes \code{NULL \%in\% known} a zero-length logical, and an
unguarded \verb{if()} around it raises "argument is of length zero" instead of
answering. The shape is checked before the comparison, never after, the
same reason \code{parse_module_response()} checks \code{contract_version}'s shape
before coercing it — a JSON array of one element parses to an R list, not
a character scalar, and would otherwise be compared (and could match)
where a scalar belongs.
}
\keyword{internal}
