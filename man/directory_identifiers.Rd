% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{directory_identifiers}
\alias{directory_identifiers}
\title{The addresses an account carries as its own identifiers}
\usage{
directory_identifiers(account)
}
\arguments{
\item{account}{One row of a directory frame.}
}
\value{
A normalized character vector, empties dropped.
}
\description{
Wider than the matching set by one field, \code{mail}, and the distinction is
deliberate: the criterion is the contact address and the design names the
two carriers it lives in, while this answers a different question — does
this account already carry the thing the referent declared?
}
\details{
\code{officeLocation} is in here as the legacy carrier and not as an identifier
of its own. It has to be: on the accounts the historical flow created \code{mail}
is \code{null} and \code{otherMails} is empty, so without it a referent who declared
the right person's alias would be told the row contradicts itself. It
retires with the fallback of decision 9.
}
\keyword{internal}
