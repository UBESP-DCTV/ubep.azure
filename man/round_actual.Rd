% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_actual}
\alias{round_actual}
\title{The real rows somebody actually asked about}
\usage{
round_actual(results, requests)
}
\arguments{
\item{results}{The \code{results} of a \code{state} reply.}

\item{requests}{The requests this instance was asked to satisfy.}
}
\value{
The subset of \code{results} whose pair appears in \code{requests}.
}
\description{
The one filter that keeps the mass revocation from being reachable.
\code{provisioning_diff()} classifies as \code{revocato} every pair present in reality
and absent from the desired state, and the state read carries the
requesters' own rows because the scope gate needed them. Handed the
unfiltered reply, the diff would propose revoking the referents.
}
\details{
Absence means nothing (decision 5) and only an explicit \code{revoked} takes
something away. This is what makes that structural rather than remembered.
}
\keyword{internal}
