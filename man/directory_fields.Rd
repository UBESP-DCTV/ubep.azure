% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_directory.R
\name{directory_fields}
\alias{directory_fields}
\title{The directory fields the sweep asks for, and the columns it hands back}
\usage{
directory_fields()
}
\value{
A character vector of Graph field names, in the order the frame's
columns take.
}
\description{
One list, read twice: it builds the \verb{$select} and it names the columns of
the frame. Two copies would be a map between two sets of names, which is
where this package keeps finding its worst bugs — and here the drift would
be silent in the worst direction, because a field asked for and not read
looks exactly like an account that does not carry it.
}
\details{
\code{User.ReadBasic.All} would have been the narrower permission and is not
enough for this list: it carries neither \code{officeLocation}, \code{otherMails},
\code{createdDateTime} nor \code{userType}, which are the four the resolution decides
on. The select list is therefore also the reason \code{User.Read.All} is the
permission asked for.
}
\keyword{internal}
