% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register.R
\name{request_from_row}
\alias{request_from_row}
\title{Turn one register row into a request}
\usage{
request_from_row(row)
}
\arguments{
\item{row}{A one-row data frame from the register.}
}
\value{
A named list holding only the fields that carry a value.
}
\description{
The register's field names are the request's field names, so there is no map
between the two and no key can fall into a default without raising. That is
the whole reason the names were chosen.
}
\details{
A blank cell is dropped rather than carried as \code{""}. The difference matters
downstream: \code{provisioning_diff()} reads an absent field as "not set", while
an empty string would be a value nobody asked for — and a DAG nobody asked
for is an update, never a \code{noop}.
}
\keyword{internal}
