% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_validate.R
\name{compose_upn}
\alias{compose_upn}
\title{Compose the user principal name used as REDCap username}
\usage{
compose_upn(first_name, last_name, domain = "ubep.unipd.it")
}
\arguments{
\item{first_name, last_name}{Person's names, in any casing or accenting.}

\item{domain}{Tenant domain.}
}
\value{
A single string.
}
\description{
The UPN is not a mailbox: it is the identity the person authenticates with,
and it doubles as the REDCap username because the instances are configured
with \code{oauth2_azure_ad_username_attribute = userPrincipalName}. The address
someone can actually be reached at travels separately, as \code{contact_email}.
}
\details{
Normalization is delegated to \code{clean_string()}, which already lowercases,
squishes, turns spaces into dots and transliterates accents to ASCII.
}
\keyword{internal}
