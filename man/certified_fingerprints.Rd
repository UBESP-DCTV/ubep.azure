% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_fingerprint.R
\name{certified_fingerprints}
\alias{certified_fingerprints}
\title{Fingerprints whose write behavior a conformance run has verified}
\usage{
certified_fingerprints(registry = tested_fingerprints())
}
\arguments{
\item{registry}{Data frame of tested fingerprints.}
}
\value{
A character vector, possibly empty.
}
\description{
The list the ordinary caller declares when it is about to write. It is
deliberately narrower than the measured one that \code{tested_fingerprints()}
returns: a row added after a bare \code{state} measures a surface, it does not
certify what writing to it does.
}
\details{
The conformance run itself declares the measured list instead, and must:
the surface it is about to certify has no date yet by definition, so
requiring one would recreate the ordering trap the ceiling already has —
the run could never earn what it exists to earn.
}
\keyword{internal}
