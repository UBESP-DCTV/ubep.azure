% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register.R
\name{register_to_desired}
\alias{register_to_desired}
\title{Turn the register into the desired state}
\usage{
register_to_desired(register)
}
\arguments{
\item{register}{The register as a data frame, one row per pair, carrying at
least \code{record_id} and \code{request_status}.}
}
\value{
A list with \code{desired}, \code{revoked} and \code{errors}. The first two hold
requests that already went through \code{intake_request()}, each carrying its
\code{record_id} so the outcome knows where to go back. \code{errors} is named by
\code{record_id}.
}
\description{
The register declares requests; it does not describe reality. A pair that
exists in REDCap and appears nowhere here means nothing, so this function
never produces a revocation that a person did not ask for: only
\code{request_status = "revoked"} does that.
}
\details{
Three things keep a row out of the desired state, and they are not the same
thing. A row with no resolved username is not a pair yet and is not an
error. A pair that appears twice is an error on \strong{both} rows, because
either one of them is the mistake and there is no way to tell which —
guessing which one wins is what a ledger does, and a register refuses. A row
that fails validation carries the data-error codes back to whoever wrote it.
}
\keyword{internal}
