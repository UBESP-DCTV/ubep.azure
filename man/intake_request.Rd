% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_validate.R
\name{intake_request}
\alias{intake_request}
\title{Take a request in, validated and normalized, or not at all}
\usage{
intake_request(request)
}
\arguments{
\item{request}{A named list, one request as described in the spec.}
}
\value{
A list with \code{errors} and \code{request}; \code{request} is \code{NULL} when
\code{errors} is not empty.
}
\description{
The single door into the pure layer. Validation and the expiration
conversion happen together because separating them would leave a way to
get a valid request that was never converted, and the way to get the
conversion wrong is to forget it.
}
\details{
From here on every component — the diff, \code{apply}, \code{before} and \code{after} —
speaks the value REDCap stores. The request speaks what a person means.
}
\keyword{internal}
