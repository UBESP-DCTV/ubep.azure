% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{directory_is_candidate}
\alias{directory_is_candidate}
\title{The rows the resolution is allowed to match against}
\usage{
directory_is_candidate(directory)
}
\arguments{
\item{directory}{A directory frame as \code{directory_users()} returns.}
}
\value{
A logical vector, one per row.
}
\description{
Guests are excluded, and by invariant rather than by measurement: whoever is
on the REDCap of UBEP has a \code{ubep.unipd.it} account, or is given one. The
tenant
holds 760 guests and they carry their external address in \code{otherMails},
which is one of the two fields searched, so a guest matching alongside a
member would produce an \code{ambiguous} that is not an ambiguity.
}
\details{
A row whose \code{userType} could not be read stays a candidate, which is not the
same as trusting it: it is kept so that it cannot silently become "nobody
matched", and \code{resolve_identity()} then refuses to grant on it. Dropping it
here would turn an unreadable field into a second account for a person who
already has one, and that is the failure this design fears most.
}
\keyword{internal}
