% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_scope.R
\name{scope_pairs}
\alias{scope_pairs}
\title{The rights an instance has to be asked about before a request is applied}
\usage{
scope_pairs(register)
}
\arguments{
\item{register}{The register as a data frame, carrying at least \code{server},
\code{project_id} and \code{requested_by}.}
}
\value{
A data frame with \code{server}, \code{project_id} and \code{username}, one row per
distinct triple. The requester travels under \code{username} because that is the
name \code{module_state()} expects in a pair.
}
\description{
The scope gate asks whether the person who filed a request may manage the
users of the project they filed it for. That question is about the
\strong{requester}, not about the person the request is for, so it needs a state
read the channel does not otherwise make: \code{module_state()} answers for the
pairs it is given, and the pairs it is given are the grantees.
}
\details{
Rows with no server or no project are not asked about. There is nothing to
ask — and \code{validate_request()} already reports the project one. A row with no
requester is not asked about either, and is refused by \code{scope_errors()}
instead: an unattributable row cannot be answered by any instance.
}
\keyword{internal}
