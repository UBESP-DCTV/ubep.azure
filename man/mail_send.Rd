% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_mail.R
\name{mail_send}
\alias{mail_send}
\title{Hand one message to the mail service}
\usage{
mail_send(api_key, from, to, subject, body, cc = NULL, reply_to = NULL)
}
\arguments{
\item{api_key}{The service key, which the runner reads from Key Vault.}

\item{from}{The verified sender identity.}

\item{to}{Recipient address.}

\item{subject, body}{The composed message.}

\item{cc}{Optional address in copy.}

\item{reply_to}{Optional address replies should reach.}
}
\value{
A list with \code{ok} and \code{errors}.
}
\description{
REDCap on the register's instance does not send over SMTP: it sends through
its mail provider's HTTP API. So does this, which is why no new dependency
appears -- the call is a JSON POST, the same shape \code{module_call()} already
uses.
}
\details{
\code{202} is what success looks like, and it means "accepted", not "delivered".
A bounce happens afterwards and asynchronously, and never comes back into
the round. What the round can promise is that it did not lose the message
through a fault of its own; it cannot promise the referent read it.

\code{from} is not a choice: the account has one verified sender identity and any
other address is refused. It arrives as an argument all the same, because a
resource name written into a public repository is a resource name published.
}
\keyword{internal}
