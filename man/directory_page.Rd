% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_directory.R
\name{directory_page}
\alias{directory_page}
\title{Fetch one page of the directory}
\usage{
directory_page(token, url)
}
\arguments{
\item{token}{A Graph access token, sent as a bearer credential in the
\code{Authorization} header and never in the URL. The managed identity holds
\code{User.Read.All} and \code{User.Create}, and neither can modify, disable or
delete an existing account.}

\item{url}{The page to read: the first one this package builds, every one
after it a continuation Graph handed back.}
}
\value{
A list with \code{ok}, \code{errors} and \code{payload}, the last being the parsed
body on success.
}
\description{
The answer is recognized by its shape and by its status, the way the
register's client does it and for the same reason: an endpoint that is not
the one meant can answer 200 with something that is not JSON, and a client
that inferred success from the status would fail inside the parser reporting
a cause that is not the cause.
}
\keyword{internal}
