% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_package.R
\name{build_redcap_module}
\alias{build_redcap_module}
\title{Build the distributable REDCap module archive}
\usage{
build_redcap_module(out_dir = ".")
}
\arguments{
\item{out_dir}{Directory the archive is written to.}
}
\value{
The path of the archive, invisibly.
}
\description{
Packs \verb{inst/redcap-module/} into the archive REDCap expects, naming the top
level directory \verb{ubep_provisioning_v<version>}. REDCap derives both the
module prefix and its version from that directory name, so the name is part
of the contract rather than a convention.
}
\details{
The module and its R client share a single version number, the one in
\code{DESCRIPTION}: the endpoint contract has two ends, and keeping them in one
repository with one version is what stops their compatibility from becoming
a matrix to remember. The \code{VERSION} file is rewritten from \code{DESCRIPTION}
while staging rather than copied, so the two cannot drift unnoticed.

The \verb{tests/} directory is left out. It is the module's own test suite, run
in CI and never needed on a server.
}
