% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_conformance.R
\name{request_for}
\alias{request_for}
\title{Build one write request from a case's assertion}
\usage{
request_for(username, project_id, assert)
}
\arguments{
\item{username}{Test account.}

\item{project_id}{Test project.}

\item{assert}{Named list of fields to assert, as in one case's \code{assert}.}
}
\value{
A named list, one element of the \code{requests} argument
\code{module_apply()} expects.
}
\description{
\code{updatePrivileges} reads whatever keys are present and skips the rest, so
an absent key is different from an asserted absence. Every case in
\code{conformance_cases()} encodes "clear this field" as an R \code{NULL}, which is
correct on the expectation side that \code{compare_readback()} reads, but wrong
on the wire: \code{utils::modifyList()} drops a \code{NULL}-valued key instead of
setting it, and even a key that did survive with a \code{NULL} value would
serialize through \code{httr2::req_body_json(auto_unbox = TRUE)} as \code{{}}, not
\code{""}. So this is the one place a \code{NULL} in \code{assert} becomes the explicit
empty string the channel elsewhere asserts an absence as.
}
\keyword{internal}
