% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_state_pairs}
\alias{round_state_pairs}
\title{The pairs one instance has to be asked about}
\usage{
round_state_pairs(requests, asks)
}
\arguments{
\item{requests}{The validated requests bound for this instance, each
carrying \code{username} and \code{project_id}.}

\item{asks}{The rows of \code{scope_pairs()} for this instance.}
}
\value{
A list of \code{list(username, project_id)}, deduplicated.
}
\description{
Two questions need a state read and they are not the same question: the diff
needs the grantees' rows, the scope gate needs the requesters'. Asking for
both in one call is not an optimization — it is what makes the gate and the
diff see the same instant of reality, instead of two instants a write could
fit between.
}
\keyword{internal}
