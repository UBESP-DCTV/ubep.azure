% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register_api.R
\name{register_metadata}
\alias{register_metadata}
\title{Read the register's data dictionary as the live project holds it}
\usage{
register_metadata(url, token)
}
\arguments{
\item{url}{Hostname of the instance hosting the register, optionally
followed by the path REDCap is mounted under. A scheme is dropped rather
than honored, so a base handed over as http is corrected instead of
silently downgrading the channel.}

\item{token}{The API token of the service account, sent in the body.}
}
\value{
The \code{register_call()} list plus \code{dictionary}, a data frame.
}
\description{
This is the caller \code{compare_dictionary()} has been waiting for: until now
the comparison existed and nothing ever handed it a live dictionary, so a
drift on the form — a field dropped, a type loosened, a \verb{@READONLY}
annotation removed — could not be seen by anything.
}
\keyword{internal}
