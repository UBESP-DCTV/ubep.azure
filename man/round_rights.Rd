% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_rights}
\alias{round_rights}
\title{The rights one instance reported, in the shape the gate consumes}
\usage{
round_rights(server, state)
}
\arguments{
\item{server}{The instance name, which a \code{state} reply names once at the top
and not on every row.}

\item{state}{What \code{module_state()} returned.}
}
\value{
A data frame with \code{server}, \code{project_id}, \code{username}, and
\code{user_rights} only when it was reported.
}
\description{
The permission column is added only when the instance actually reported it.
Inventing a column of NAs would reach \code{scope_errors()}'s closed failure by a
different route and make the two cases indistinguishable upstream, where
they have to be told apart.
}
\keyword{internal}
