% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_directory.R
\name{directory_frame}
\alias{directory_frame}
\title{Turn parsed Graph user records into a frame of one row per account}
\usage{
directory_frame(users)
}
\arguments{
\item{users}{The parsed \code{value} array, a list of one list per account.}
}
\value{
A data frame with one row per account and the columns
\code{directory_fields()} names, in that order.
}
\description{
Every column keeps the type the wire gives it, which is not the same as
guessing one. Eight of the nine fields arrive as strings and stay character,
with a field the record does not carry becoming \code{""} rather than \code{NA} — the
absence is what it means, and 1.170 accounts of the tenant have no
\code{officeLocation}. \code{accountEnabled} is a JSON boolean and stays logical:
carried as the string \code{"FALSE"} it would be a value every \verb{if ()} reads as
false and every \code{nzchar()} reads as present. \code{otherMails} is the one field
that is an array on the wire and stays a list column, because collapsing it
would leave the resolution splitting it back apart on a separator no rule
says an address cannot contain.
}
\keyword{internal}
