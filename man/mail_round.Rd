% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_mail.R
\name{mail_round}
\alias{mail_round}
\title{Send this round's messages, and say which rows may now be written}
\usage{
mail_round(
  changed,
  register,
  born,
  mailer,
  dry_run,
  redirect_to = NULL,
  copy_to = NULL,
  reply_to = NULL,
  identified = NULL
)
}
\arguments{
\item{changed}{The rows this round would write, from \code{round_changed()}.}

\item{register}{The register as read, carrying every field the messages
name.}

\item{born}{The accounts created this round: lists with \code{record_id}, \code{upn}
and \code{credential}.}

\item{mailer}{\verb{function(to, cc, subject, body)} returning \code{ok} and \code{errors}.}

\item{dry_run}{When \code{TRUE} nothing is sent and every row comes back
writable -- the round behaves as it did before this file existed.}

\item{redirect_to}{One address replacing every recipient, for a field test.}

\item{copy_to}{Address in copy, on the outcome message only.}

\item{reply_to}{Kept for the caller's symmetry with \code{mail_send()}; the
mailer closure is what carries it to the transport.}

\item{identified}{The identity this round settled, as a frame with
\code{record_id} and the fields it settled. \code{register} is the register as
read, on purpose; this is how the message names what the round has just
established rather than what the referent typed.}
}
\value{
A list with \code{recapitate}, \code{errori} and \code{contatori}.
}
\description{
The order is the decision: send first, write after. A row whose message did
not leave stays as the register has it, so the next round finds it changed
again and retries -- the queue is the register, and no second state exists
to keep aligned. The cost accepted in exchange is a duplicate, never a loss.
}
\details{
The credential message is the asymmetry. The account is already born, the
round does not create it twice, and the managed identity cannot repair it.
So its failure is not a retry: it is a person's job, and it goes out under
its own code so an alarm can tell the two apart.
}
\keyword{internal}
