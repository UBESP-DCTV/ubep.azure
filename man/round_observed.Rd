% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_observed}
\alias{round_observed}
\title{What the instance showed for one written pair}
\usage{
round_observed(reread, entry)
}
\arguments{
\item{reread}{The \code{results} of the post-write \code{state} call, or \code{NULL} when
this was a simulation and there is nothing to read back.}

\item{entry}{One entry of a write response's \code{results}, carrying
\code{username}, \code{project_id} and the response's own \code{after}.}
}
\value{
The matching row from \code{reread}, the entry's own \code{after} when
\code{reread} is \code{NULL}, or \code{NULL} when a real re-read ran and did not find
the pair.
}
\description{
The one place that decides what a write's \code{applied_as} is allowed to come
from. A \code{NULL} re-read means there was nothing to read back — a
simulation, where the module's own \code{after} is the whole story, because
nothing happened to look at. A re-read that ran carries the only thing
that can settle whether the write landed: what the instance shows now, not
what the write claimed a moment ago.
}
\keyword{internal}
