% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_register.R
\name{seal_payload}
\alias{seal_payload}
\title{Build the body that writes a seal back into the register}
\usage{
seal_payload(record_id, seal, state)
}
\arguments{
\item{record_id}{The register record to write to.}

\item{seal}{The seal of what was applied, \code{""} when nothing was.}

\item{state}{One of \code{seal_state_vocabulary()}.}
}
\value{
A one-row data frame with exactly the seal columns.
}
\description{
A third door beside the outcome and the identity, and it exists for the
reason those two are separate rather than for a new one: the columns are
fixed here so a bug cannot rewrite anything else.
}
\details{
It could not be a column of \code{outcome_payload()}, and the reason is worth
keeping. That body is written on every outcome, so a seal column would carry
a value on every outcome too — and the honest default is empty. A
\code{data_error} on an already applied row would then blank the seal, quietly
taking the protection off the one kind of row that has any.
}
\keyword{internal}
