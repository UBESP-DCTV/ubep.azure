% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_client.R
\name{module_state}
\alias{module_state}
\title{Read the real authorization state from one REDCap instance}
\usage{
module_state(
  server,
  secret,
  pairs = list(),
  registry = tested_fingerprints(),
  declare = certified_fingerprints(registry)
)
}
\arguments{
\item{server}{Hostname of the instance, optionally followed by the path
REDCap is mounted under, as in \code{"host.example.org/redcap"}. The fleet is
not uniform on this point, so the mount cannot be assumed.}

\item{secret}{Shared secret, sent as the \code{X-UBEP-Secret} header.}

\item{pairs}{List of (username, project_id) pairs; empty reads the whole
instance, which is what the audit needs.}

\item{registry}{Tested fingerprints, see \code{check_fingerprint()}.}

\item{declare}{Fingerprints the caller states it has been tested against.
The module refuses to write when its own is not among them, so an empty
declaration writes nothing. Reads and dry runs ignore it.}
}
\value{
A list with \code{ok}, \code{errors}, \code{payload} and \code{gate}.
}
\description{
Everything it returns is re-read state: the channel keeps no local copy,
because a copy would only be authoritative if nobody could edit REDCap
outside the job, which is neither true nor desirable.
}
\details{
The request travels as a JSON body, not in the query string. Two reasons:
the assertions of the write operations would not fit in a URL, and in a GET
every UPN would land in the access log of each instance on every run. The
module declares its page in \code{no-csrf-pages} so the POST is not rejected —
see the spec for why CSRF protection does not apply to this endpoint.
}
\keyword{internal}
