% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{confirm_identity}
\alias{confirm_identity}
\title{Decide whether the single match really is the person the row means}
\usage{
confirm_identity(account, declared, previous)
}
\arguments{
\item{account}{The single matching row of the directory frame.}

\item{declared}{The declared UPN, normalized and possibly filled in from an
internal contact address. Empty once a verdict exists on the row, because
then the register's \code{username} is this round's own earlier answer and not
a declaration to confirm.}

\item{previous}{The previous verdict, normalized.}
}
\value{
A list with \code{identity}, \code{username} and \code{errors}.
}
\description{
A match on the contact address is where the confirmation starts, not where
it ends. Three things can stop it, and the prefix on each is a delivery
address rather than a label: \code{DATO_} closes the row against whoever filed it
and mails them, \code{TRASPORTO_} keeps it queued and mails us.
}
\details{
The two \code{TRASPORTO_} ones are fields the account does not carry, and neither
is the filer's to fix. Telling the one person who cannot reach a directory
attribute that they are not authorized sends them to go and argue — which is
the same reasoning \code{scope_errors()} gives for its fourth case.
}
\keyword{internal}
