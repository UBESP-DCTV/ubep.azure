% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_batches}
\alias{round_batches}
\title{Split what has to be written into batches of one project each}
\usage{
round_batches(entries)
}
\arguments{
\item{entries}{Validated requests, each carrying \code{record_id}, \code{server} and
\code{project_id}.}
}
\value{
A list of batches, each \code{list(server, project_id, record_ids, requests)}, where \code{requests} carry only the five fields the module reads.
}
\description{
\code{api.php} refuses a write that touches more than one \code{project_id}, and
refuses it outright before touching anything: \code{PROJECT_ID} is an immutable
per-process \code{define()}, and it is what makes every write land in its own
project's log. A job that grouped by instance would take that refusal on its
first mixed batch, and the diagnosis would start from the wrong place.
}
\details{
Simulations are batched the same way, although the module lets them span
projects. One rule instead of two, and a simulation that exercises exactly
the batching the write will use: a rehearsal shaped differently from the
performance cannot warn about it.
}
\keyword{internal}
