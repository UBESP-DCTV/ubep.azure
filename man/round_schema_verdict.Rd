% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_round.R
\name{round_schema_verdict}
\alias{round_schema_verdict}
\title{Which dictionary differences stop the round and which are only reported}
\usage{
round_schema_verdict(differences)
}
\arguments{
\item{differences}{The \code{differences} of \code{compare_dictionary()}.}
}
\value{
A list with \code{blocks}, \code{blocking} and \code{tolerated}.
}
\description{
Five of the seven codes change what the round reads and one of them —
a \verb{@READONLY} that fell — means a requester may have typed \code{applied} into
the outcome, so the register could be carrying a success nobody produced.
The other two cannot: a field the round ignores does not corrupt its
reading, and choices that could not be compared are the declared
consequence of calling without the fleet list.
}
\keyword{internal}
