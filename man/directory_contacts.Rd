% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_identity.R
\name{directory_contacts}
\alias{directory_contacts}
\title{The addresses a directory row can be found by}
\usage{
directory_contacts(directory)
}
\arguments{
\item{directory}{A directory frame as \code{directory_users()} returns.}
}
\value{
A list with one character vector per row, normalized, with empties
dropped so that an empty criterion cannot match an empty field.
}
\description{
Both carriers are read: \code{officeLocation}, where the historical flow put the
contact address and where 6.494 accounts still carry it, and \code{otherMails},
the proper carrier this package writes from now on. The fallback retires
when the sweep counts zero accounts with an address in the first — a number,
not an intention, and one the sweep produces by itself every round.
}
\keyword{internal}
