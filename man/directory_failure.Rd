% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_directory.R
\name{directory_failure}
\alias{directory_failure}
\title{Say what went wrong, and hand back no directory at all}
\usage{
directory_failure(code, payload = NULL)
}
\arguments{
\item{code}{The transport code, one of the \verb{TRASPORTO_DIRECTORY_*} family.}

\item{payload}{Anything worth keeping for a diagnosis, or \code{NULL}.}
}
\value{
A list with \code{ok}, \code{errors}, \code{payload} and \code{users}.
}
\description{
A half-read sweep is not a small sweep. Every account on a page that did not
arrive is missing, and a missing account resolves to \code{absent}, which is the
verdict that opens the creation branch — so partial rows escaping from here
would turn a transport failure into duplicate people. \code{users} is \code{NULL} on
every failure for that reason, and it is what keeps "I could not ask"
distinguishable from "nobody is there".
}
\keyword{internal}
