% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provisioning_conformance.R
\name{record_conformance}
\alias{record_conformance}
\title{Record a passed conformance run in the registry}
\usage{
record_conformance(path, major, on)
}
\arguments{
\item{path}{Registry CSV.}

\item{major}{REDCap major the run covered.}

\item{on}{Date of the run.}
}
\value{
The updated registry, invisibly.
}
\description{
Written by the run itself rather than by hand: a trigger that depends on
someone remembering to type a date is a reminder, which is the thing the
mechanism exists to replace.
}
\details{
Called with \code{path} defaulted from \code{run_conformance_check()}, so the same
caveat applies here: under \code{devtools::load_all()} the default resolves
inside the source tree and the write lands where the next task's \verb{git add} can see it, while against an installed copy it lands in the
installation library instead and never reaches the repository.
}
\keyword{internal}
